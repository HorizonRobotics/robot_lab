resnet
======

.. py:module:: robo_orchard_lab.models.modules.resnet


Attributes
----------

.. autoapisummary::

   robo_orchard_lab.models.modules.resnet.logger


Classes
-------

.. autoapisummary::

   robo_orchard_lab.models.modules.resnet.BasicBlock
   robo_orchard_lab.models.modules.resnet.Bottleneck
   robo_orchard_lab.models.modules.resnet.ResNet


Functions
---------

.. autoapisummary::

   robo_orchard_lab.models.modules.resnet.conv3x3
   robo_orchard_lab.models.modules.resnet.make_res_layer


Module Contents
---------------

.. py:data:: logger

.. py:function:: conv3x3(in_planes: int, out_planes: int, stride: int = 1, dilation: int = 1)

   3x3 convolution with padding.


.. py:class:: BasicBlock(inplanes: int, planes: int, stride: int = 1, dilation: int = 1, downsample: Optional[torch.nn.Module] = None, style: str = 'pytorch', with_cp: bool = False, use_reentrant: bool = False)

   Bases: :py:obj:`torch.nn.Module`


   Base class for all neural network modules.

   Your models should also subclass this class.

   Modules can also contain other Modules, allowing to nest them in
   a tree structure. You can assign the submodules as regular attributes::

       import torch.nn as nn
       import torch.nn.functional as F

       class Model(nn.Module):
           def __init__(self) -> None:
               super().__init__()
               self.conv1 = nn.Conv2d(1, 20, 5)
               self.conv2 = nn.Conv2d(20, 20, 5)

           def forward(self, x):
               x = F.relu(self.conv1(x))
               return F.relu(self.conv2(x))

   Submodules assigned in this way will be registered, and will have their
   parameters converted too when you call :meth:`to`, etc.

   .. note::
       As per the example above, an ``__init__()`` call to the parent class
       must be made before assignment on the child.

   :ivar training: Boolean represents whether this module is in training or
                   evaluation mode.
   :vartype training: bool


   .. py:attribute:: expansion
      :value: 1



   .. py:attribute:: conv1


   .. py:attribute:: bn1


   .. py:attribute:: relu


   .. py:attribute:: conv2


   .. py:attribute:: bn2


   .. py:attribute:: downsample
      :value: None



   .. py:attribute:: stride
      :value: 1



   .. py:attribute:: dilation
      :value: 1



   .. py:attribute:: with_cp
      :value: False



   .. py:attribute:: use_reentrant
      :value: False



   .. py:method:: forward(x)

      Forward function.



.. py:class:: Bottleneck(inplanes: int, planes: int, stride: int = 1, dilation: int = 1, downsample: Optional[torch.nn.Module] = None, style: str = 'pytorch', with_cp: bool = False)

   Bases: :py:obj:`torch.nn.Module`


   Base class for all neural network modules.

   Your models should also subclass this class.

   Modules can also contain other Modules, allowing to nest them in
   a tree structure. You can assign the submodules as regular attributes::

       import torch.nn as nn
       import torch.nn.functional as F

       class Model(nn.Module):
           def __init__(self) -> None:
               super().__init__()
               self.conv1 = nn.Conv2d(1, 20, 5)
               self.conv2 = nn.Conv2d(20, 20, 5)

           def forward(self, x):
               x = F.relu(self.conv1(x))
               return F.relu(self.conv2(x))

   Submodules assigned in this way will be registered, and will have their
   parameters converted too when you call :meth:`to`, etc.

   .. note::
       As per the example above, an ``__init__()`` call to the parent class
       must be made before assignment on the child.

   :ivar training: Boolean represents whether this module is in training or
                   evaluation mode.
   :vartype training: bool


   .. py:attribute:: expansion
      :value: 4



   .. py:attribute:: conv1


   .. py:attribute:: conv2


   .. py:attribute:: bn1


   .. py:attribute:: bn2


   .. py:attribute:: conv3


   .. py:attribute:: bn3


   .. py:attribute:: relu


   .. py:attribute:: downsample
      :value: None



   .. py:attribute:: stride
      :value: 1



   .. py:attribute:: dilation
      :value: 1



   .. py:attribute:: with_cp
      :value: False



   .. py:method:: forward(x: torch.Tensor) -> torch.Tensor


.. py:function:: make_res_layer(block: torch.nn.Module, inplanes: int, planes: int, blocks: int, stride: int = 1, dilation: int = 1, style: str = 'pytorch', with_cp: bool = False) -> torch.nn.Module

.. py:class:: ResNet(depth: int, in_channels=3, base_channels=64, num_stages: int = 4, strides: Sequence[int] = (1, 2, 2, 2), dilations: Sequence[int] = (1, 1, 1, 1), out_indices: Sequence[int] = (0, 1, 2, 3), style: str = 'pytorch', frozen_stages: int = -1, bn_eval: bool = True, bn_frozen: bool = False, with_cp: bool = False)

   Bases: :py:obj:`torch.nn.Module`


   ResNet backbone.

   :param depth: Depth of resnet, from {18, 34, 50, 101, 152}.
   :type depth: int
   :param num_stages: Resnet stages, normally 4.
   :type num_stages: int
   :param strides: Strides of the first block of each stage.
   :type strides: Sequence[int]
   :param dilations: Dilation of each stage.
   :type dilations: Sequence[int]
   :param out_indices: Output from which stages.
   :type out_indices: Sequence[int]
   :param style: `pytorch` or `caffe`. If set to "pytorch", the stride-two
                 layer is the 3x3 conv layer, otherwise the stride-two layer is
                 the first 1x1 conv layer.
   :type style: str
   :param frozen_stages: Stages to be frozen (all param fixed). -1 means
                         not freezing any parameters.
   :type frozen_stages: int
   :param bn_eval: Whether to set BN layers as eval mode, namely, freeze
                   running stats (mean and var).
   :type bn_eval: bool
   :param bn_frozen: Whether to freeze weight and bias of BN layers.
   :type bn_frozen: bool
   :param with_cp: Use checkpoint or not. Using checkpoint will save some
                   memory while slowing down the training speed.
   :type with_cp: bool


   .. py:attribute:: arch_settings


   .. py:attribute:: out_indices
      :value: (0, 1, 2, 3)



   .. py:attribute:: style
      :value: 'pytorch'



   .. py:attribute:: frozen_stages
      :value: -1



   .. py:attribute:: bn_eval
      :value: True



   .. py:attribute:: bn_frozen
      :value: False



   .. py:attribute:: with_cp
      :value: False



   .. py:attribute:: inplanes
      :type:  int
      :value: 64



   .. py:attribute:: conv1


   .. py:attribute:: bn1


   .. py:attribute:: relu


   .. py:attribute:: maxpool


   .. py:attribute:: res_layers
      :value: []



   .. py:attribute:: feat_dim


   .. py:method:: init_weights(pretrained: Optional[str] = None) -> None


   .. py:method:: forward(x: torch.Tensor) -> Union[torch.Tensor, Tuple[torch.Tensor]]


   .. py:method:: train(mode: bool = True) -> None

      Set the module in training mode.

      This has any effect only on certain modules. See documentations of
      particular modules for details of their behaviors in training/evaluation
      mode, if they are affected, e.g. :class:`Dropout`, :class:`BatchNorm`,
      etc.

      :param mode: whether to set training mode (``True``) or evaluation
                   mode (``False``). Default: ``True``.
      :type mode: bool

      :returns: self
      :rtype: Module



