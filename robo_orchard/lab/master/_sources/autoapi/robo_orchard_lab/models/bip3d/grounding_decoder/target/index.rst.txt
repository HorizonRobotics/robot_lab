target
======

.. py:module:: robo_orchard_lab.models.bip3d.grounding_decoder.target


Classes
-------

.. autoapisummary::

   robo_orchard_lab.models.bip3d.grounding_decoder.target.Grounding3DTarget


Module Contents
---------------

.. py:class:: Grounding3DTarget(cls_weight=1.0, alpha=0.25, gamma=2, eps=1e-12, box_weight=1.0, num_dn=0, dn_noise_scale=0.5, add_neg_dn=True, with_dn_query=False, num_classes=None, embed_dims=256, label_noise_scale=0.5, cost_weight_wd=1.0, cost_weight_cd=0.8, use_ignore_mask=False)

   Bases: :py:obj:`torch.nn.Module`


   Base class for all neural network modules.

   Your models should also subclass this class.

   Modules can also contain other Modules, allowing them to be nested in
   a tree structure. You can assign the submodules as regular attributes::

       import torch.nn as nn
       import torch.nn.functional as F

       class Model(nn.Module):
           def __init__(self) -> None:
               super().__init__()
               self.conv1 = nn.Conv2d(1, 20, 5)
               self.conv2 = nn.Conv2d(20, 20, 5)

           def forward(self, x):
               x = F.relu(self.conv1(x))
               return F.relu(self.conv2(x))

   Submodules assigned in this way will be registered, and will also have their
   parameters converted when you call :meth:`to`, etc.

   .. note::
       As per the example above, an ``__init__()`` call to the parent class
       must be made before assignment on the child.

   :ivar training: Boolean represents whether this module is in training or
                   evaluation mode.
   :vartype training: bool


   .. py:attribute:: num_dn
      :value: 0



   .. py:attribute:: cls_weight
      :value: 1.0



   .. py:attribute:: box_weight
      :value: 1.0



   .. py:attribute:: alpha
      :value: 0.25



   .. py:attribute:: gamma
      :value: 2



   .. py:attribute:: eps
      :value: 1e-12



   .. py:attribute:: dn_noise_scale
      :value: 0.5



   .. py:attribute:: add_neg_dn
      :value: True



   .. py:attribute:: with_dn_query
      :value: False



   .. py:attribute:: cost_weight_wd
      :value: 1.0



   .. py:attribute:: cost_weight_cd
      :value: 0.8



   .. py:attribute:: use_ignore_mask
      :value: False



   .. py:method:: encode_reg_target(box_target, device=None)


   .. py:method:: sample(cls_pred, box_pred, char_positives, box_target, text_dict, ignore_mask=None)


   .. py:method:: get_dn_anchors(char_positives, box_target, text_dict, label=None, ignore_mask=None)


