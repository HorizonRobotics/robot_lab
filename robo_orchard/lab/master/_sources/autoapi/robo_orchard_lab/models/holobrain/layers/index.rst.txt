layers
======

.. py:module:: robo_orchard_lab.models.holobrain.layers


Classes
-------

.. autoapisummary::

   robo_orchard_lab.models.holobrain.layers.ScalarEmbedder
   robo_orchard_lab.models.holobrain.layers.RotaryEmbedding
   robo_orchard_lab.models.holobrain.layers.RotaryAttention
   robo_orchard_lab.models.holobrain.layers.JointGraphAttention
   robo_orchard_lab.models.holobrain.layers.TemporalJointGraphAttention
   robo_orchard_lab.models.holobrain.layers.AdaRMSNorm
   robo_orchard_lab.models.holobrain.layers.UpsampleHead


Functions
---------

.. autoapisummary::

   robo_orchard_lab.models.holobrain.layers.linear_act_ln
   robo_orchard_lab.models.holobrain.layers.rotate_half


Module Contents
---------------

.. py:function:: linear_act_ln(embed_dims, in_loops, out_loops, input_dims=None, act_cfg=None)

.. py:class:: ScalarEmbedder(hidden_size, frequency_embedding_size=256, max_period=10000)

   Bases: :py:obj:`torch.nn.Module`


   Base class for all neural network modules.

   Your models should also subclass this class.

   Modules can also contain other Modules, allowing to nest them in
   a tree structure. You can assign the submodules as regular attributes::

       import torch.nn as nn
       import torch.nn.functional as F

       class Model(nn.Module):
           def __init__(self) -> None:
               super().__init__()
               self.conv1 = nn.Conv2d(1, 20, 5)
               self.conv2 = nn.Conv2d(20, 20, 5)

           def forward(self, x):
               x = F.relu(self.conv1(x))
               return F.relu(self.conv2(x))

   Submodules assigned in this way will be registered, and will have their
   parameters converted too when you call :meth:`to`, etc.

   .. note::
       As per the example above, an ``__init__()`` call to the parent class
       must be made before assignment on the child.

   :ivar training: Boolean represents whether this module is in training or
                   evaluation mode.
   :vartype training: bool


   .. py:attribute:: mlp


   .. py:attribute:: frequency_embedding_size
      :value: 256



   .. py:attribute:: max_period
      :value: 10000



   .. py:attribute:: freqs


   .. py:method:: forward(t)


.. py:function:: rotate_half(x)

   Rotates half the hidden dims of the input.


.. py:class:: RotaryEmbedding(dim, max_position_embeddings=2048, base=10000, device=None, scaling_factor=1.0)

   Bases: :py:obj:`torch.nn.Module`


   Base class for all neural network modules.

   Your models should also subclass this class.

   Modules can also contain other Modules, allowing to nest them in
   a tree structure. You can assign the submodules as regular attributes::

       import torch.nn as nn
       import torch.nn.functional as F

       class Model(nn.Module):
           def __init__(self) -> None:
               super().__init__()
               self.conv1 = nn.Conv2d(1, 20, 5)
               self.conv2 = nn.Conv2d(20, 20, 5)

           def forward(self, x):
               x = F.relu(self.conv1(x))
               return F.relu(self.conv2(x))

   Submodules assigned in this way will be registered, and will have their
   parameters converted too when you call :meth:`to`, etc.

   .. note::
       As per the example above, an ``__init__()`` call to the parent class
       must be made before assignment on the child.

   :ivar training: Boolean represents whether this module is in training or
                   evaluation mode.
   :vartype training: bool


   .. py:attribute:: scaling_factor
      :value: 1.0



   .. py:attribute:: dim


   .. py:attribute:: max_position_embeddings
      :value: 2048



   .. py:attribute:: max_seq_len_cached
      :value: 2048



   .. py:attribute:: base
      :value: 10000



   .. py:attribute:: inv_freq


   .. py:attribute:: cos


   .. py:attribute:: sin


   .. py:method:: forward(x, position_ids)


.. py:class:: RotaryAttention(embed_dims, num_heads=8, qkv_bias=True, qk_scale=None, max_position_embeddings=128)

   Bases: :py:obj:`torch.nn.Module`


   Base class for all neural network modules.

   Your models should also subclass this class.

   Modules can also contain other Modules, allowing to nest them in
   a tree structure. You can assign the submodules as regular attributes::

       import torch.nn as nn
       import torch.nn.functional as F

       class Model(nn.Module):
           def __init__(self) -> None:
               super().__init__()
               self.conv1 = nn.Conv2d(1, 20, 5)
               self.conv2 = nn.Conv2d(20, 20, 5)

           def forward(self, x):
               x = F.relu(self.conv1(x))
               return F.relu(self.conv2(x))

   Submodules assigned in this way will be registered, and will have their
   parameters converted too when you call :meth:`to`, etc.

   .. note::
       As per the example above, an ``__init__()`` call to the parent class
       must be made before assignment on the child.

   :ivar training: Boolean represents whether this module is in training or
                   evaluation mode.
   :vartype training: bool


   .. py:attribute:: num_heads
      :value: 8



   .. py:attribute:: scale


   .. py:attribute:: q_proj


   .. py:attribute:: k_proj


   .. py:attribute:: v_proj


   .. py:attribute:: proj


   .. py:attribute:: position_encoder


   .. py:method:: init_weights()


   .. py:method:: forward(query, key, value=None, query_pos=None, key_pos=None, attn_mask=None, pre_scale=True, key_padding_mask=None, identity=None, **kwargs)


   .. py:method:: apply_position_encode(q, query_pos, k, key_pos)


.. py:class:: JointGraphAttention(embed_dims, num_heads=8, qkv_bias=True, qk_scale=None)

   Bases: :py:obj:`torch.nn.Module`


   Base class for all neural network modules.

   Your models should also subclass this class.

   Modules can also contain other Modules, allowing to nest them in
   a tree structure. You can assign the submodules as regular attributes::

       import torch.nn as nn
       import torch.nn.functional as F

       class Model(nn.Module):
           def __init__(self) -> None:
               super().__init__()
               self.conv1 = nn.Conv2d(1, 20, 5)
               self.conv2 = nn.Conv2d(20, 20, 5)

           def forward(self, x):
               x = F.relu(self.conv1(x))
               return F.relu(self.conv2(x))

   Submodules assigned in this way will be registered, and will have their
   parameters converted too when you call :meth:`to`, etc.

   .. note::
       As per the example above, an ``__init__()`` call to the parent class
       must be made before assignment on the child.

   :ivar training: Boolean represents whether this module is in training or
                   evaluation mode.
   :vartype training: bool


   .. py:attribute:: num_heads
      :value: 8



   .. py:attribute:: scale


   .. py:attribute:: q_proj


   .. py:attribute:: k_proj


   .. py:attribute:: v_proj


   .. py:attribute:: proj


   .. py:attribute:: position_encoder


   .. py:method:: init_weights()


   .. py:method:: forward(query, key=None, value=None, query_pos=None, identity=None, **kwargs)


.. py:class:: TemporalJointGraphAttention(embed_dims, num_heads=8, qkv_bias=True, qk_scale=None, max_position_embeddings=None)

   Bases: :py:obj:`torch.nn.Module`


   Base class for all neural network modules.

   Your models should also subclass this class.

   Modules can also contain other Modules, allowing to nest them in
   a tree structure. You can assign the submodules as regular attributes::

       import torch.nn as nn
       import torch.nn.functional as F

       class Model(nn.Module):
           def __init__(self) -> None:
               super().__init__()
               self.conv1 = nn.Conv2d(1, 20, 5)
               self.conv2 = nn.Conv2d(20, 20, 5)

           def forward(self, x):
               x = F.relu(self.conv1(x))
               return F.relu(self.conv2(x))

   Submodules assigned in this way will be registered, and will have their
   parameters converted too when you call :meth:`to`, etc.

   .. note::
       As per the example above, an ``__init__()`` call to the parent class
       must be made before assignment on the child.

   :ivar training: Boolean represents whether this module is in training or
                   evaluation mode.
   :vartype training: bool


   .. py:attribute:: num_heads
      :value: 8



   .. py:attribute:: scale


   .. py:attribute:: q_proj


   .. py:attribute:: k_proj


   .. py:attribute:: v_proj


   .. py:attribute:: proj


   .. py:attribute:: joint_pos_encoder


   .. py:attribute:: temporal_position_encoder


   .. py:method:: init_weights()


   .. py:method:: forward(query, key=None, value=None, joint_distance=None, temporal_pos_q=None, temporal_pos_k=None, temporal_attn_mask=None, identity=None, **kwargs)


.. py:class:: AdaRMSNorm(normalized_shape, condition_dims, num_condition_mlp_layers=2, elementwise_affine=False, eps=1e-06, zero=False, **kwargs)

   Bases: :py:obj:`torch.nn.RMSNorm`


   Applies Root Mean Square Layer Normalization over a mini-batch of inputs.

   This layer implements the operation as described in
   the paper `Root Mean Square Layer Normalization <https://arxiv.org/pdf/1910.07467.pdf>`__

   .. math::
       y = \frac{x}{\sqrt{\mathrm{RMS}[x] + \epsilon}} * \gamma

   The root mean squared norm is taken over the last ``D`` dimensions, where ``D``
   is the dimension of :attr:`normalized_shape`. For example, if :attr:`normalized_shape`
   is ``(3, 5)`` (a 2-dimensional shape), the rms norm is computed over
   the last 2 dimensions of the input.

   :param normalized_shape: input shape from an expected input
                            of size

                            .. math::
                                [* \times \text{normalized\_shape}[0] \times \text{normalized\_shape}[1]
                                    \times \ldots \times \text{normalized\_shape}[-1]]

                            If a single integer is used, it is treated as a singleton list, and this module will
                            normalize over the last dimension which is expected to be of that specific size.
   :type normalized_shape: int or list or torch.Size
   :param eps: a value added to the denominator for numerical stability. Default: :func:`torch.finfo(x.dtype).eps`
   :param elementwise_affine: a boolean value that when set to ``True``, this module
                              has learnable per-element affine parameters initialized to ones (for weights)
                              and zeros (for biases). Default: ``True``.

   Shape:
       - Input: :math:`(N, *)`
       - Output: :math:`(N, *)` (same shape as input)

   Examples::

       >>> rms_norm = nn.RMSNorm([2, 3])
       >>> input = torch.randn(2, 2, 3)
       >>> rms_norm(input)



   .. py:attribute:: zero
      :value: False



   .. py:attribute:: adaLN_modulation


   .. py:method:: forward(x, c)

      Runs forward pass.



   .. py:method:: apply_ada_func(x, c)


.. py:class:: UpsampleHead(upsample_sizes, input_dim, dims, norm, act, norm_act_idx, num_output_layers=-1, out_dim=8)

   Bases: :py:obj:`torch.nn.Module`


   Base class for all neural network modules.

   Your models should also subclass this class.

   Modules can also contain other Modules, allowing to nest them in
   a tree structure. You can assign the submodules as regular attributes::

       import torch.nn as nn
       import torch.nn.functional as F

       class Model(nn.Module):
           def __init__(self) -> None:
               super().__init__()
               self.conv1 = nn.Conv2d(1, 20, 5)
               self.conv2 = nn.Conv2d(20, 20, 5)

           def forward(self, x):
               x = F.relu(self.conv1(x))
               return F.relu(self.conv2(x))

   Submodules assigned in this way will be registered, and will have their
   parameters converted too when you call :meth:`to`, etc.

   .. note::
       As per the example above, an ``__init__()`` call to the parent class
       must be made before assignment on the child.

   :ivar training: Boolean represents whether this module is in training or
                   evaluation mode.
   :vartype training: bool


   .. py:attribute:: norm_act_idx


   .. py:attribute:: upsamples


   .. py:attribute:: convs


   .. py:attribute:: act_and_norm


   .. py:attribute:: num_output_layers
      :value: -1



   .. py:method:: forward(x)


