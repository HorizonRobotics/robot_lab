vision_encoder
==============

.. py:module:: robo_orchard_lab.models.monodream.multimodal_encoder.vision_encoder


Classes
-------

.. autoapisummary::

   robo_orchard_lab.models.monodream.multimodal_encoder.vision_encoder.VisionTower
   robo_orchard_lab.models.monodream.multimodal_encoder.vision_encoder.SiglipVisionTower


Module Contents
---------------

.. py:class:: VisionTower(vision_tower, args, delay_load=False)

   Bases: :py:obj:`torch.nn.Module`


   Base class for all neural network modules.

   Your models should also subclass this class.

   Modules can also contain other Modules, allowing to nest them in
   a tree structure. You can assign the submodules as regular attributes::

       import torch.nn as nn
       import torch.nn.functional as F

       class Model(nn.Module):
           def __init__(self) -> None:
               super().__init__()
               self.conv1 = nn.Conv2d(1, 20, 5)
               self.conv2 = nn.Conv2d(20, 20, 5)

           def forward(self, x):
               x = F.relu(self.conv1(x))
               return F.relu(self.conv2(x))

   Submodules assigned in this way will be registered, and will have their
   parameters converted too when you call :meth:`to`, etc.

   .. note::
       As per the example above, an ``__init__()`` call to the parent class
       must be made before assignment on the child.

   :ivar training: Boolean represents whether this module is in training or
                   evaluation mode.
   :vartype training: bool


   .. py:attribute:: is_loaded
      :value: False



   .. py:attribute:: vision_tower_name


   .. py:attribute:: select_layer


   .. py:attribute:: select_feature


   .. py:attribute:: cfg_only
      :value: None



   .. py:method:: feature_select(image_forward_outs)


   .. py:method:: forward(images)


   .. py:property:: dummy_feature


   .. py:property:: dtype


   .. py:property:: device


   .. py:property:: config


   .. py:property:: hidden_size


   .. py:property:: num_patches


.. py:class:: SiglipVisionTower(vision_cfg: str, config: transformers.PretrainedConfig)

   Bases: :py:obj:`VisionTower`


   Base class for all neural network modules.

   Your models should also subclass this class.

   Modules can also contain other Modules, allowing to nest them in
   a tree structure. You can assign the submodules as regular attributes::

       import torch.nn as nn
       import torch.nn.functional as F

       class Model(nn.Module):
           def __init__(self) -> None:
               super().__init__()
               self.conv1 = nn.Conv2d(1, 20, 5)
               self.conv2 = nn.Conv2d(20, 20, 5)

           def forward(self, x):
               x = F.relu(self.conv1(x))
               return F.relu(self.conv2(x))

   Submodules assigned in this way will be registered, and will have their
   parameters converted too when you call :meth:`to`, etc.

   .. note::
       As per the example above, an ``__init__()`` call to the parent class
       must be made before assignment on the child.

   :ivar training: Boolean represents whether this module is in training or
                   evaluation mode.
   :vartype training: bool


   .. py:attribute:: vision_tower


