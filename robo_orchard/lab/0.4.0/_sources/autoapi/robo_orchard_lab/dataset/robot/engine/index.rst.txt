engine
======

.. py:module:: robo_orchard_lab.dataset.robot.engine

.. autoapi-nested-parse::

   The database engine utilities that wrap SQLAlchemy.

   For database engine which is used specifically for RoboOrchard dataset,
   see :py:mod:`robo_orchard_lab.dataset.robot.dataset_db_engine`.



Functions
---------

.. autoapisummary::

   robo_orchard_lab.dataset.robot.engine.create_tables
   robo_orchard_lab.dataset.robot.engine.drop_tables
   robo_orchard_lab.dataset.robot.engine.create_engine
   robo_orchard_lab.dataset.robot.engine.create_temp_engine


Module Contents
---------------

.. py:function:: create_tables(engine: sqlalchemy.Engine, base: Type[sqlalchemy.orm.DeclarativeBase], tables: Sequence[sqlalchemy.Table] | None = None, checkfirst: bool = True)

.. py:function:: drop_tables(engine: sqlalchemy.Engine, base: Type[sqlalchemy.orm.DeclarativeBase], tables: Sequence[sqlalchemy.Table] | None = None, checkfirst: bool = True)

   Drop tables in the database.

   :param engine: The database engine.
   :type engine: Engine
   :param base: The base class of the ORM models.
   :type base: Type[DeclarativeBase]
   :param tables: The tables to drop. If None,
                  all tables in the base are dropped. Defaults to None.
   :type tables: Sequence[Table], optional
   :param checkfirst: Whether to check if the tables exist
                      before dropping them. Defaults to True.
   :type checkfirst: bool, optional


.. py:function:: create_engine(url: sqlalchemy.URL, readonly: bool = False, json_serializer: Callable[[Any], str] = lambda obj: json.dumps(obj, sort_keys=True, ensure_ascii=True), **kwargs) -> sqlalchemy.Engine

   Create a database engine.

   This method is a wrapper of :py:func:`sqlalchemy.create_engine` with
   additional support for readonly mode, and default JSON serializer.

   See :py:func:`sqlalchemy.create_engine` for more details.

   :param url: The database URL.
   :type url: URL
   :param readonly: Whether to create a readonly engine.
                    Defaults to False.
   :type readonly: bool, optional
   :param json_serializer: The JSON serializer
                           for the engine. Defaults to
                           `lambda obj: json.dumps(obj, sort_keys=True, ensure_ascii=True).`
   :type json_serializer: Callable[[Any], str], optional
   :param \*\*kwargs: Additional keyword arguments for
                      :py:func:`sqlalchemy.create_engine`.

   :returns: The database engine.
   :rtype: Engine


.. py:function:: create_temp_engine(dir: str = os.path.abspath('./'), prefix: str = 'duckdb_temp', drivername: str = 'duckdb', **kwargs) -> Generator[sqlalchemy.Engine, Any, None]

   Create a temporary SQLite engine.


